import pygments.token as token
print(token.STANDARD_TYPES)

#All you need to do now is to add all the token types and make sure that hhighlighting works fully for
#python,java,javascript,typescript,html,css, and if those work, than other languages probably work too
#Then you can finally be free and release the update